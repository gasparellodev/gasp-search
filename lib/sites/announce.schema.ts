/**
 * Alias de export (#231 — nome canônico da issue) para o schema Zod do
 * formulário Anunciar. Fonte única continua em `announcement.schema.ts`.
 */
export {
  AnnouncementSchema,
  type AnnouncementInput,
} from "@/lib/sites/announcement.schema";
